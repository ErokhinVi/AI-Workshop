[project]
name = "raif-dashboard"
version = "0.1.0"
description = "Live dashboard для AI-воркшопа правления Райффайзен (3 мая 2026)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
]

[tool.ruff]
line-length = 100
target-version = "py311"
